#include <iostream>
#include <vector>
#include <random>
//#include "request.h"
//#include "courier.h"

int main() {
    // some code for gui
/*
    while (true) {
        // add random time for waiting
        Request *request = new Request();
        Dispatcher dispatcher(request);
    }
    */
   std::cout << "ibjuf\n";
}
